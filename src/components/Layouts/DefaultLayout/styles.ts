import {styled} from '../../../styles/theme'

export const LayoutContainer = styled('div', {
  '.alignCenterContainer': {
    width: '100%',
    maxWidth: '70rem',

    marginLeft: 'auto',
    marginRight: 'auto',
  }
})